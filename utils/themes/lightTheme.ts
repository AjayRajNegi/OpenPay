import { colorPalette } from "./colorPalette";
import { AppTheme } from "./types";

const { white, blue } = colorPalette;

export const LightTheme: AppTheme = {
  bgColor: {
    primary: blue[50],
    secondary: white,
  },
  // TODO
};
